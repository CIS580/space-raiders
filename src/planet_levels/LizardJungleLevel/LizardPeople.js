export default class LizardPeople {
  constructor(x, y, direction, text, tileset) {
    this.x = x;
    this.y = y;
    this.direction = direction;
    this.text = text;
    this.tileset = tileset;
  }

  update() {
    
  }

  render(ctx) {
    //use new draw tile function
  }
}
